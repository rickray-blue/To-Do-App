import React from 'react';
import "./style.css";
import Logo from "../Logo"

const Header = props => {
    return (
        <header className="Header">
            <Logo />
        </header>
    );
};

export default Header;
