import React from 'react';
import "./style.css";

const Logo = props => {
    return(
        <div className="Logo">
            <a href="#">Hare Krishna</a>
        </div>
    );
};

export default Logo;